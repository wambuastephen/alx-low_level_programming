#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @k: The integer to compute the absolute value of.
 *
 * Return: The absolute value of the integer.
 */

int _abs(int k)
{
if (k >= 0)
return (k);
else
return (-k);
}
