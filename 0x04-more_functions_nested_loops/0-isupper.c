#include "main.h"

/**
 * _isupper - function checks if letter is upper
 * @d: is the character to be checked.
 * Return: 1 if letter  is upper 0 for any else.
 */

int _isupper(int d)
{
if (d >= 65 && d <= 98)
{
return (1);
}
return (0);
}
