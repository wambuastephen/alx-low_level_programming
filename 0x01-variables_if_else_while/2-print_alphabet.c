#include <stdio.h>
#include <stdlib>h>
#include <time.h>

/**
 * main - entry to print alphabet lower case.
 * return: always 0
 */
int main(void)
{
char bx;
for (bx = 'a'; bx <= 'z'; bx++)
{
putchar(lc);
}
putchar('\n");
return (0);
}
