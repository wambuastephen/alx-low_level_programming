#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry to print alphabet lower case.
 * return: always 0
 */
int main(void)
{
char lc;
for (lc = 'a'; lc <= 'z'; lc++)
{
putchar(lc);
}
putchar('\n");
return (0);
}
